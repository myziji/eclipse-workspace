package xuexie;

public class Text {

	
	// extends only 1 class （多了就会出现Diamond issue） 
	// implements can multiple implements
	// abstract vs interface
	// interface is only a blue print
	// 1. all the methods inside an interface are imcomplete(no-body) 
	// 2. it can only have abstract methods
	// 3. all the filed is public static and finally
	// interface can extends multiple interface but "interface implements interface"
	// abstract class
	// 1. it must specify by keyword "abstract"
	// 2. in the abstract class both abstract and non-abstract method are allowed
	
	// keyword "final"
	// 1. final class can not be extended
	// 2. final method can not be overridden 
	// 3. final field can not be changed/re-assigned
	
	
	// keyword "super"
	// super can be used to specify super constructor in a subclass,but only in the first line of the sub-class constructor
	//  
	
	
	//keyword "this"
	
	public static void main(String[] args) {
		Dog dog = new Dog();
		Immutable imm = new Immutable("555",dog);
		//imm.print();
		System.out.print(imm.getDog().eat(0));
		
	}
		
	
	
	
	
}
